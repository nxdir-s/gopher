package {{.Package}}

import "testing"

func Test{{.Name.Pascal}}(t *testing.T) {
	tests := map[string]struct {
		input    string
		expected string
		wantErr  bool
	}{
{{- if .Lists.case}}
{{- range .Lists.case}}
		"{{.}}": {},
{{- end}}
{{- else}}
		"": {},
{{- end}}
	}

	for name, test := range tests {
		t.Run(name, func(t *testing.T) {
			// TODO: exercise {{.Name.Pascal}} with test.input

			if test.wantErr {
				t.Skip("not implemented")
			}

			if test.expected != "" {
				t.Errorf("got %q, want %q", test.input, test.expected)
			}
		})
	}
}
